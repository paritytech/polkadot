[package]
name = "polkadot-simnet-test"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
edition = "2018"

[dependencies]
# substrate primitives
sp-core = { git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }
sp-blockchain = { git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }
sp-runtime = { git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }

# frame pallets
system = { package = "frame-system", git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }
balances = { package = "pallet-balances", git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }

# substrate client libs
sc-client-api = { git = "https://github.com/paritytech/substrate", branch = "kiz-ocw-max-heap-pages" }

# polkadot deps
polkadot-simnet = { path = "../common" }
polkadot-runtime = { path = "../../../../runtime/polkadot" }
