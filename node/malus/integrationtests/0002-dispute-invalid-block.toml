[settings.defaults]
image = "{{get_env(name="SYNTHIMAGE") | safe }}"
command = "polkadot"
chain-name = "polkadot-local"
timeout = 1000

[nodes.alice]
validator = true
image = "{{get_env(name="SYNTHIMAGE") | safe }}"
command = "polkadot"
extra-args = ["--alice"]

[nodes.bob]
validator = true
image = "{{get_env(name="MALUSIMAGE") | safe }}"
command = "/usr/local/bin/malus"
subcommand = "back-garbage-candidate"
extra-args = ["--bob"]

[nodes.charlie]
validator = true
image = "{{get_env(name="MALUSIMAGE") | safe }}"
command = "/usr/local/bin/malus"
subcommand = "back-garbage-candidate"
extra-args = ["--charlie"]

[nodes.david]
validator = true
image = "{{get_env(name="MALUSIMAGE") | safe }}"
command = "/usr/local/bin/malus"
subcommand = "back-garbage-candidate"
extra-args = ["--dave"]

[nodes.eve]
validator = true
image = "{{get_env(name="MALUSIMAGE") | safe }}"
command = "/usr/local/bin/malus"
subcommand = "suggest-garbage-candidate"
extra-args = ["--eve"]
