[package]
name = "polkadot-executor"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Polkadot node implementation in Rust."

[dependencies]
substrate-executor = { git = "https://github.com/paritytech/substrate", branch = "rh-generic-inherent" }
substrate-primitives = { git = "https://github.com/paritytech/substrate", branch = "rh-generic-inherent" }
polkadot-runtime = { path = "../runtime" }
