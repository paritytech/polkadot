[package]
name = "test-runtime-constants"
version.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
smallvec = "1.8.0"

frame-support = { git = "https://github.com/davxy/substrate", branch = "move-beefy-to-consensus", default-features = false }
primitives = { package = "polkadot-primitives", path = "../../../primitives", default-features = false }
runtime-common = { package = "polkadot-runtime-common", path = "../../common", default-features = false }
sp-runtime = { git = "https://github.com/davxy/substrate", default-features = false , branch = "move-beefy-to-consensus" }
sp-weights = { git = "https://github.com/davxy/substrate", default-features = false , branch = "move-beefy-to-consensus" }
sp-core = { git = "https://github.com/davxy/substrate", default-features = false , branch = "move-beefy-to-consensus" }

[features]
default = ["std"]
std = [
	"sp-core/std",
	"sp-runtime/std",
	"sp-weights/std"
]
