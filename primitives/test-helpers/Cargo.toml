[package]
name = "polkadot-primitives-test-helpers"
version = "0.9.26"
authors = ["Parity Technologies <admin@parity.io>"]
edition = "2021"

[dependencies]
sp-keyring = { git = "https://github.com/paritytech/substrate", branch = "oty-bump-heap-pages" }
sp-application-crypto = { package = "sp-application-crypto", git = "https://github.com/paritytech/substrate", default-features = false , branch = "oty-bump-heap-pages" }
sp-runtime = { git = "https://github.com/paritytech/substrate", branch = "oty-bump-heap-pages" }
polkadot-primitives = { path = "../" }
rand = "0.8.5"
