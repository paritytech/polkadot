name: Release-candidate automation
on:
  push:
    branches:
      - v[0-9]+.[0-9]+.[0-9]+
jobs:
  tag_rc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - id: compute_tag
        name: Compute next rc tag
        shell: bash
        run: |
          # Get last rc tag if exists, else set it to {branch-name}-rc1
          branch=${GITHUB_REF#refs/heads/}
          echo "$branch"
          echo "::set-output name=branch_name::$branch"
          git tag -l
          last_rc=$(git tag -l "$branch-rc*" | sort -V | tail -n 1)
          if [ -n "$last_rc" ]; then
            suffix=$(echo "$last_rc" | grep -Eo '[0-9]+$')
            echo $suffix
            ((suffix++))
            echo $suffix
            echo "::set-output name=new_tag::$branch-rc$suffix"
            echo "::set-output name=first_rc::false"
          else
            echo "::set-output name=new_tag::$branch-rc1"
            echo "::set-output name=first_rc::true"
          fi
      - name: Apply new tag
        uses: s3krit-testorg/github-tagger@v0.0.2
        with:
          # We can't use the normal GITHUB_TOKEN for the following reason:
          # https://docs.github.com/en/actions/reference/events-that-trigger-workflows#triggering-new-workflows-using-a-personal-access-token
          # RELEASE_BRANCH_TOKEN requires public_repo OAuth scope
          repo-token: "${{ secrets.RELEASE_BRANCH_TOKEN }}"
          tag: ${{ steps.compute_tag.outputs.new_tag }}
      - id: create-issue
        uses: JasonEtco/create-an-issue@v2
        # Only create the issue if it's the first release candidate
        if: steps.compute_tag.outputs.first_rc == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: ${{ steps.compute_tag.outputs.branch_name }}
        with:
          filename: .github/ISSUE_TEMPLATE/release.md
      - uses: s3krit/matrix-message-action@v0.0.2
        with:
          room_id: ${{ secrets.INTERNAL_POLKADOT_MATRIX_ROOM_ID }}
          access_token: ${{ secrets.MATRIX_ACCESS_TOKEN }}
          server: "matrix.parity.io"
          message: "Release process for polkadot ${{ steps.compute_tag.outputs.branch_name }} has been started. Tracking issue: ${{ steps.create-issue.outputs.url }}"
